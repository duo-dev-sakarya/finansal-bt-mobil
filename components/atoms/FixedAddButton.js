import React from 'react'
import { Button } from 'react-native'

const FixedAddButton = ({onPress}) => {
  return (
    <Button onPress={onPress} title="FixedAddButton"/>
  )
}

export default FixedAddButton